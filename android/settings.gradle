pluginManagement {
    def flutterSdkPath = System.getenv("FLUTTER_ROOT") ?: System.getenv("CODEMAGIC_FLUTTER_ROOT") ?: "/Users/builder/flutter"
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
plugins {
    id "com.android.application" version "8.1.0" apply false
    id "org.jetbrains.kotlin.android" version "1.9.0" apply false
}
include ":app"